package pageObjects.products;

import org.openqa.selenium.WebDriver;

public class NoneShoesProductListPage extends ProductListPage{

    public NoneShoesProductListPage(WebDriver webDriver) {
        super(webDriver);
    }
}
